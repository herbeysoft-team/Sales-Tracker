{
  "name": "functions",
  "description": "Cloud Functions for Ledger — sends WhatsApp alerts via CallMeBot when the sales ledger changes",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.3.1",
    "firebase-functions": "^5.0.1"
  },
  "private": true
}
